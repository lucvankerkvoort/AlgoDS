/**
 * LeetCode 198. House Robber
 *
 * Houses are in a row, each with some money. You can't rob two adjacent
 * houses. Maximize the total amount robbed.
 *
 * Hint: at each house, decide to skip it or rob it (its value + best up
 * to two houses back). Roll a running pair of bests instead of an array.
 * Aim for O(n) time, O(1) space.
 */

export function rob(nums: number[]): number {
  throw new Error("Not implemented");
}
