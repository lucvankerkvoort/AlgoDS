/**
 * LeetCode 152. Maximum Product Subarray
 *
 * Find the contiguous subarray with the largest product and return that
 * product.
 *
 * Hint: like Kadane's for sums, but a negative number can flip the
 * smallest product into the largest -- track both a running max and
 * running min ending at each index.
 * Aim for O(n) time, O(1) space.
 */

export function maxProduct(nums: number[]): number {
  throw new Error("Not implemented");
}
