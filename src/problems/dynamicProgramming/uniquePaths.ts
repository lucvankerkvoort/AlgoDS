/**
 * LeetCode 62. Unique Paths
 *
 * A robot starts at the top-left of an m x n grid and can only move down
 * or right. Count the distinct paths to the bottom-right corner.
 *
 * Hint: dp[r][c] = dp[r-1][c] + dp[r][c-1]. Roll into a single row to
 * save space. Aim for O(m * n) time.
 */

export function uniquePaths(m: number, n: number): number {
  throw new Error("Not implemented");
}
