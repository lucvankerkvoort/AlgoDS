/**
 * LeetCode 621. Task Scheduler
 *
 * Given tasks (letters) and a cooldown `n` between two same-type tasks,
 * return the minimum number of time units (including idle slots) to
 * finish all tasks.
 *
 * Example:
 *   tasks = ["A","A","A","B","B","B"], n = 2 -> 8
 *   (A B idle A B idle A B)
 *
 * Hint: the most frequent task dictates the schedule's shape. Think about
 * how many "cooldown chunks" the most frequent task forces.
 */

export function leastInterval(tasks: string[], n: number): number {
  throw new Error("Not implemented");
}
