/**
 * LeetCode 215. Kth Largest Element in an Array
 *
 * Return the kth largest element (not the kth distinct one) in an
 * unsorted array.
 *
 * Example:
 *   [3,2,1,5,6,4], k = 2 -> 5
 *
 * Hint: maintain a min-heap of size k as you scan the array.
 * Aim for O(n log k).
 */

export function findKthLargest(nums: number[], k: number): number {
  throw new Error("Not implemented");
}
