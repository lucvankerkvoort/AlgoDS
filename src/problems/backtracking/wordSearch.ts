/**
 * LeetCode 79. Word Search
 *
 * Given a 2D grid of letters and a word, determine if the word can be
 * constructed from letters of sequentially adjacent cells (horizontally
 * or vertically), using each cell at most once.
 *
 * Hint: DFS/backtracking from every cell matching the word's first
 * letter, marking cells visited on the current path and unmarking on
 * backtrack.
 */

export function exist(board: string[][], word: string): boolean {
  throw new Error("Not implemented");
}
