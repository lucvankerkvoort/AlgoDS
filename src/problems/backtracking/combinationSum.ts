/**
 * LeetCode 39. Combination Sum
 *
 * Given distinct candidates and a target, return all unique combinations
 * where the chosen numbers sum to target. The same number may be reused
 * unlimited times.
 *
 * Hint: sort candidates, then backtrack -- at each step either reuse the
 * same index (allowing repeats) or move forward, pruning once the
 * running sum exceeds target.
 */

export function combinationSum(candidates: number[], target: number): number[][] {
  throw new Error("Not implemented");
}
