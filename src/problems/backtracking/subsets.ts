/**
 * LeetCode 78. Subsets
 *
 * Return all possible subsets (the power set) of a set of distinct
 * integers.
 *
 * Example:
 *   [1,2,3] -> [[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]] (any order)
 *
 * Hint: backtracking -- at each starting index, record the current path,
 * then try extending it with every later element.
 */

export function subsets(nums: number[]): number[][] {
  throw new Error("Not implemented");
}
