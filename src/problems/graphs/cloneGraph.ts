/**
 * LeetCode 133. Clone Graph
 *
 * Given a reference to a node in a connected undirected graph, return a
 * deep copy (clone) of the graph.
 *
 * Hint: DFS with a map from original node -> clone, created *before*
 * recursing into neighbors so cycles don't cause infinite recursion.
 * Aim for O(V + E) time.
 */

import { GraphNode } from "../../common/graphNode";

export function cloneGraph(node: GraphNode | null): GraphNode | null {
  throw new Error("Not implemented");
}
