/**
 * LeetCode 127. Word Ladder
 *
 * Given a `beginWord`, `endWord`, and a word list, find the length of the
 * shortest transformation sequence from beginWord to endWord, changing
 * one letter at a time, where every intermediate word must be in the
 * word list. Return 0 if no such sequence exists.
 *
 * Hint: BFS over the implicit graph where an edge connects two words
 * differing by exactly one letter. Try swapping every position to every
 * letter a-z rather than comparing against every other word.
 * Aim for O(n * L * 26) time.
 */

export function ladderLength(beginWord: string, endWord: string, wordList: string[]): number {
  throw new Error("Not implemented");
}
