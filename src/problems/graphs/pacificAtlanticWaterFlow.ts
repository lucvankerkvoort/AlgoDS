/**
 * LeetCode 417. Pacific Atlantic Water Flow
 *
 * Water can flow from a cell to a 4-directional neighbor with height <=
 * current height. The Pacific touches the top/left edges, the Atlantic
 * the bottom/right edges. Return all cells from which water can reach
 * both oceans.
 *
 * Hint: reverse the flow -- DFS/BFS "uphill" from every ocean-adjacent
 * border cell, once per ocean, then intersect the two reachable sets.
 * Aim for O(rows * cols) time.
 */

export function pacificAtlantic(heights: number[][]): number[][] {
  throw new Error("Not implemented");
}
