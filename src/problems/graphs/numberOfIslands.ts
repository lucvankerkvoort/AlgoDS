/**
 * LeetCode 200. Number of Islands
 *
 * Given a 2D grid of '1' (land) and '0' (water), count the number of
 * islands (connected groups of land, 4-directionally).
 *
 * Hint: flood fill (DFS/BFS) from every unvisited land cell, marking the
 * whole island visited as you go.
 * Aim for O(rows * cols) time.
 */

export function numIslands(grid: string[][]): number {
  throw new Error("Not implemented");
}
