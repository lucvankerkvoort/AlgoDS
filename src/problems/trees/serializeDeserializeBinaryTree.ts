/**
 * LeetCode 297. Serialize and Deserialize Binary Tree
 *
 * Design an algorithm to serialize a binary tree to a string and
 * deserialize it back to the original tree structure.
 *
 * Hint: pre-order DFS, encoding null children with a sentinel ("#"). A
 * single sentinel is enough to reconstruct the exact structure.
 * Aim for O(n) time for both directions.
 */

import { TreeNode } from "../../common/treeNode";

export function serialize(root: TreeNode | null): string {
  throw new Error("Not implemented");
}

export function deserialize(data: string): TreeNode | null {
  throw new Error("Not implemented");
}
