/**
 * LeetCode 235. Lowest Common Ancestor of a Binary Search Tree
 *
 * Given a BST and two of its nodes p and q, find their lowest common
 * ancestor.
 *
 * Hint: exploit BST ordering -- no need for general-tree LCA logic here.
 * Aim for O(h) time, O(1) space (iterative).
 */

import { TreeNode } from "../../common/treeNode";

export function lowestCommonAncestor(root: TreeNode, p: TreeNode, q: TreeNode): TreeNode {
  throw new Error("Not implemented");
}
