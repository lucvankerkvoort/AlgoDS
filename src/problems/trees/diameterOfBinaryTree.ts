/**
 * LeetCode 543. Diameter of Binary Tree
 *
 * The diameter is the length (in edges) of the longest path between any
 * two nodes, which may or may not pass through the root.
 *
 * Hint: post-order DFS returning each subtree's height, updating a
 * running best for leftHeight + rightHeight as a side effect.
 * Aim for O(n) time.
 */

import { TreeNode } from "../../common/treeNode";

export function diameterOfBinaryTree(root: TreeNode | null): number {
  throw new Error("Not implemented");
}
