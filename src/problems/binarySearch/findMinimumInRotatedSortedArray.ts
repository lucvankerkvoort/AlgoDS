/**
 * LeetCode 153. Find Minimum in Rotated Sorted Array
 *
 * A sorted array (distinct values) has been rotated at an unknown pivot.
 * Find the minimum element in O(log n).
 *
 * Hint: compare the middle element to the rightmost element to decide
 * which side the rotation point (and thus the minimum) is on.
 */

export function findMin(nums: number[]): number {
  throw new Error("Not implemented");
}
