/**
 * LeetCode 33. Search in Rotated Sorted Array
 *
 * A sorted array has been rotated at an unknown pivot. Find the index of
 * `target` in O(log n), or return -1.
 *
 * Example:
 *   [4,5,6,7,0,1,2], target = 0 -> 4
 *
 * Hint: modified binary search -- at least one half (lo..mid or mid..hi)
 * is always normally sorted; figure out which, then check if target
 * falls in that half's range.
 */

export function search(nums: number[], target: number): number {
  throw new Error("Not implemented");
}
