/**
 * LeetCode 134. Gas Station
 *
 * There are n gas stations in a circle; gas[i] is the fuel available at
 * station i, cost[i] is the fuel needed to travel from station i to
 * i+1. Return the starting station index from which you can complete the
 * circuit, or -1 if impossible (the answer is guaranteed unique if it
 * exists).
 *
 * Hint: if total gas >= total cost, a solution is guaranteed to exist.
 * Track a running tank balance; reset the candidate start whenever it
 * goes negative.
 */

export function canCompleteCircuit(gas: number[], cost: number[]): number {
  throw new Error("Not implemented");
}
