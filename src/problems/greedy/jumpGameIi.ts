/**
 * LeetCode 45. Jump Game II
 *
 * `nums[i]` is the max jump length from index i. Return the minimum
 * number of jumps to reach the last index (guaranteed reachable).
 *
 * Hint: greedy BFS-by-levels -- track the current jump's reachable
 * boundary and the farthest reachable with one more jump.
 * Aim for O(n) time.
 */

export function jump(nums: number[]): number {
  throw new Error("Not implemented");
}
