/**
 * LeetCode 191. Number of 1 Bits
 *
 * Count the number of set bits (population count) in an unsigned 32-bit
 * integer.
 *
 * Hint: Brian Kernighan's trick -- `n & (n - 1)` clears the lowest set bit.
 */

export function hammingWeight(n: number): number {
  throw new Error("Not implemented");
}
