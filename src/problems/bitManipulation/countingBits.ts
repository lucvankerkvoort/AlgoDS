/**
 * LeetCode 338. Counting Bits
 *
 * For every number from 0 to n, return the count of set bits, as an
 * array.
 *
 * Hint: DP building on smaller results. `i >> 1` drops the lowest bit,
 * and `i & 1` tells you whether that dropped bit was 1.
 * Aim for O(n) time.
 */

export function countBits(n: number): number[] {
  throw new Error("Not implemented");
}
