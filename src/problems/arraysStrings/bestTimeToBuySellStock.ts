/**
 * LeetCode 121. Best Time to Buy and Sell Stock
 *
 * `prices[i]` is the price of a stock on day i. Choose a single day to buy
 * and a later day to sell to maximize profit. Return the max profit, or 0
 * if none is possible.
 *
 * Example:
 *   prices = [7, 1, 5, 3, 6, 4] -> 5  (buy at 1, sell at 6)
 *
 * Aim for O(n) time, O(1) space (one pass).
 */

export function maxProfit(prices: number[]): number {
  throw new Error("Not implemented");
}
