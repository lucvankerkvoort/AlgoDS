/**
 * LeetCode 238. Product of Array Except Self
 *
 * Return an array `answer` where answer[i] is the product of all elements
 * of `nums` except nums[i] -- without using division, in O(n).
 *
 * Example:
 *   [1, 2, 3, 4] -> [24, 12, 8, 6]
 *
 * Hint: one pass accumulating prefix products, another accumulating
 * suffix products.
 */

export function productExceptSelf(nums: number[]): number[] {
  throw new Error("Not implemented");
}
