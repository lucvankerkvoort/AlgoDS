/**
 * LeetCode 20. Valid Parentheses
 *
 * Given a string containing just '(){}[]', determine if it's valid: every
 * open bracket must be closed by the same type, in the correct order.
 *
 * Hint: a stack. Aim for O(n) time.
 */

export function isValid(s: string): boolean {
  throw new Error("Not implemented");
}
