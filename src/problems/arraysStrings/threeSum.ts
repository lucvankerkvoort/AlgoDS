/**
 * LeetCode 15. 3Sum
 *
 * Find all unique triplets [nums[i], nums[j], nums[k]] (distinct indices)
 * that sum to zero.
 *
 * Example:
 *   [-1, 0, 1, 2, -1, -4] -> [[-1, -1, 2], [-1, 0, 1]]
 *
 * Hint: sort first, then fix one element and two-pointer the rest.
 * Watch out for duplicate triplets. Aim for O(n^2).
 */

export function threeSum(nums: number[]): number[][] {
  throw new Error("Not implemented");
}
