/**
 * LeetCode 42. Trapping Rain Water
 *
 * Given elevation heights, compute how much water is trapped after
 * raining.
 *
 * Example:
 *   [0,1,0,2,1,0,1,3,2,1,2,1] -> 6
 *
 * Hint: two pointers from both ends, tracking max height seen so far from
 * each side. Aim for O(n) time, O(1) space.
 */

export function trap(height: number[]): number {
  throw new Error("Not implemented");
}
