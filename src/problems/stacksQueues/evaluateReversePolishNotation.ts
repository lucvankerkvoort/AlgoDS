/**
 * LeetCode 150. Evaluate Reverse Polish Notation
 *
 * Evaluate an arithmetic expression given in postfix (RPN) form, where
 * tokens are either integers or one of + - * /.
 *
 * Example:
 *   ["2","1","+","3","*"] -> 9   ((2 + 1) * 3)
 *
 * Hint: a stack -- push numbers, and on an operator pop the two most
 * recent operands. Division should truncate toward zero.
 */

export function evalRPN(tokens: string[]): number {
  throw new Error("Not implemented");
}
