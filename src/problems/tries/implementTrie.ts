/**
 * LeetCode 208. Implement Trie (Prefix Tree)
 *
 * Implement insert, search (exact word), and startsWith (prefix) for a
 * trie. See data_structures/trie.ts for the underlying structure this
 * mirrors.
 *
 * Aim for O(m) per operation, m = length of the word/prefix.
 */

export class Trie {
  insert(word: string): void {
    throw new Error("Not implemented");
  }

  search(word: string): boolean {
    throw new Error("Not implemented");
  }

  startsWith(prefix: string): boolean {
    throw new Error("Not implemented");
  }
}
